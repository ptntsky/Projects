        double d1, d2;
        if (!(cin >> d1 >> d2) || d1 != (int)d1 || d2 != (int)d2) {
            cin.clear();
            while (cin.get() != '\n');
            cout << "Помилка, введіть коректні цілі числа\n";
            continue;
        }
        first = (int)d1;
        second = (int)d2;
